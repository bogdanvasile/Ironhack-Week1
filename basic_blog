class Blog
	def initialize(all_posts)
		@all_posts = []
    end
    def add_post(post) 
    	@all_posts << post
    end
    def create_front_page
    end

    def publish_front_page
    end
end

class Post 
    attr_accessor :title, :date, :text

	def initialize(title, date, text)
		@title = title
		@date = date
		@text = text
	end

	def set_title(title)
      puts title
	end

	def set_date(date)
	end

	def set_text(text)
	end
end

new_blog = Blog.new("first blog")
new_blog.add_post Post.new("I love music", "June 15", "Where can I start") 
puts new_blog.inspect
